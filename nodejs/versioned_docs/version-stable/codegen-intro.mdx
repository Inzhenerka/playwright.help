---
id: codegen-intro
title: "Генерация тестов"
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import HTMLCard from '@site/src/components/HTMLCard';

## Введение

Playwright предоставляет возможность генерировать тесты прямо "из коробки", что является отличным способом быстро начать тестирование. Он откроет два окна: окно браузера, в котором вы взаимодействуете с веб-сайтом, который хотите протестировать, и окно Playwright Inspector, где вы можете записывать свои тесты, копировать их, очищать, а также изменять язык ваших тестов.

**Вы узнаете**
- [Как записать тест](/codegen.mdx#recording-a-test)
- [Как генерировать локаторы](/codegen.mdx#generating-locators)

## Запуск Codegen

Используйте команду `codegen`, чтобы запустить генератор тестов, за которой следует URL-адрес веб-сайта, для которого вы хотите сгенерировать тесты. URL-адрес является необязательным, и вы всегда можете запустить команду без него, а затем добавить URL-адрес непосредственно в окно браузера.

```bash
npx playwright codegen demo.playwright.dev/todomvc
```

### Запись теста

Запустите `codegen` и выполняйте действия в браузере. Playwright сгенерирует код для пользовательских взаимодействий. `Codegen` будет анализировать отрисованную страницу и определять рекомендуемый локатор, приоритизируя локаторы по роли, тексту и идентификатору теста. Если генератор обнаружит несколько элементов, соответствующих локатору, он улучшит локатор, чтобы сделать его устойчивым и уникально идентифицировать целевой элемент, тем самым устраняя и уменьшая количество сбоев тестов из-за локаторов.

С помощью генератора тестов вы можете записывать:
* Действия, такие как клик или ввод, просто взаимодействуя со страницей
* Утверждения, нажав на одну из иконок на панели инструментов, а затем кликнув на элемент на странице, с которым нужно провести утверждение. Вы можете выбрать:
  * `'assert visibility'` для утверждения, что элемент видим
  * `'assert text'` для утверждения, что элемент содержит определенный текст
  * `'assert value'` для утверждения, что элемент имеет определенное значение

###### 
![Запись теста](https://github.com/microsoft/playwright/assets/13063165/34a79ea1-639e-4cb3-8115-bfdc78e3d34d)

###### 
Когда вы закончите взаимодействовать со страницей, нажмите кнопку `'record'`, чтобы остановить запись, и используйте кнопку `'copy'`, чтобы скопировать сгенерированный код в ваш редактор.

Используйте кнопку `'clear'`, чтобы очистить код и начать запись заново. После завершения закройте окно Playwright Inspector или остановите команду терминала.

Чтобы узнать больше о генерации тестов, ознакомьтесь с нашим подробным руководством по [Codegen](./codegen.mdx).

### Генерация локаторов

Вы можете генерировать [локаторы](/locators.mdx) с помощью генератора тестов.
* Нажмите кнопку `'Record'`, чтобы остановить запись, и появится кнопка `'Pick Locator'`.
* Нажмите на кнопку `'Pick Locator'`, а затем наведите курсор на элементы в окне браузера, чтобы увидеть выделенный локатор под каждым элементом.
* Чтобы выбрать локатор, кликните на элемент, который вы хотите локализовать, и код для этого локатора появится в игровом пространстве локаторов рядом с кнопкой Pick Locator.
* Затем вы можете отредактировать локатор в игровом пространстве локаторов, чтобы уточнить его и увидеть выделенный соответствующий элемент в окне браузера.
* Используйте кнопку копирования, чтобы скопировать локатор и вставить его в ваш код.

###### 
![выбор локатора](https://github.com/microsoft/playwright/assets/13063165/2c8a12e2-4e98-4fdd-af92-1d73ae696d86)

### Эмуляция

Вы также можете генерировать тесты с использованием эмуляции, чтобы создать тест для конкретного представления, устройства, цветовой схемы, а также эмулировать геолокацию, язык или часовой пояс. Генератор тестов также может генерировать тест, сохраняя состояние аутентификации. Ознакомьтесь с руководством по [Генератору тестов](./codegen.mdx#emulation), чтобы узнать больше.

## Что дальше
- [Посмотреть трассировку ваших тестов](./trace-viewer-intro.mdx)