---  
id: library  
title: "Библиотека"
---  
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import HTMLCard from '@site/src/components/HTMLCard';

## Введение

Библиотека Playwright предоставляет единый API для запуска и взаимодействия с браузерами, тогда как Playwright Test включает в себя это же API плюс полнофункциональный тестовый фреймворк с управляемым запуском тестов.

В большинстве случаев для сквозного тестирования следует использовать `@playwright/test` (Playwright Test), а не `playwright` (Playwright Library) напрямую. Чтобы начать работу с Playwright Test, следуйте [руководству по началу работы](./intro.mdx).

## Отличия при использовании библиотеки

### Пример использования библиотеки

Следующий пример демонстрирует использование библиотеки Playwright напрямую для запуска Chromium, перехода на страницу и проверки её заголовка:

<Tabs groupId="js-flavor" defaultValue="ts" values={[ {label: 'TypeScript', value: 'ts'}, {label: 'JavaScript', value: 'js'} ]}>
    <TabItem value="ts">

        ```js
        import { chromium, devices } from 'playwright';
        import assert from 'node:assert';

        (async () => {
        // Настройка
        const browser = await chromium.launch();
        const context = await browser.newContext(devices['iPhone 11']);
        const page = await context.newPage();

        // Основная логика
        await context.route('**.jpg', route => route.abort());
        await page.goto('https://example.com/');

        assert(await page.title() === 'Example Domain'); // 👎 не Web-First утверждение

        // Завершение работы
        await context.close();
        await browser.close();
    })();
        ```

    </TabItem>
    <TabItem value="js">

        ```js
        const assert = require('node:assert');
        const { chromium, devices } = require('playwright');

        (async () => {
        // Настройка
        const browser = await chromium.launch();
        const context = await browser.newContext(devices['iPhone 11']);
        const page = await context.newPage();

        // Основная логика
        await context.route('**.jpg', route => route.abort());
        await page.goto('https://example.com/');

        assert(await page.title() === 'Example Domain'); // 👎 не Web-First утверждение

        // Завершение работы
        await context.close();
        await browser.close();
    })();
        ```

    </TabItem>
</Tabs>  

Запуск осуществляется командой `node my-script.js`.

### Пример теста

Эквивалентный тест с использованием Playwright Test выглядел бы так:

<Tabs groupId="js-flavor" defaultValue="ts" values={[ {label: 'TypeScript', value: 'ts'}, {label: 'JavaScript', value: 'js'} ]}>
    <TabItem value="ts">

        ```js
        import { expect, test, devices } from '@playwright/test';

        test.use(devices['iPhone 11']);

        test('should be titled', async ({ page, context }) => {
        await context.route('**.jpg', route => route.abort());
        await page.goto('https://example.com/');

        await expect(page).toHaveTitle('Example');
    });
        ```

    </TabItem>
    <TabItem value="js">

        ```js
        const { expect, test, devices } = require('@playwright/test');

        test.use(devices['iPhone 11']);

        test('should be titled', async ({ page, context }) => {
        await context.route('**.jpg', route => route.abort());
        await page.goto('https://example.com/');

        await expect(page).toHaveTitle('Example');
    });
        ```

    </TabItem>
</Tabs>  

Запуск осуществляется командой `npx playwright test`.

### Ключевые различия

Основные отличия приведены в таблице:

| | Библиотека | Тест |  
| - | - | - |  
| Установка | `npm install playwright` | `npm init playwright@latest` – обратите внимание на разницу между `install` и `init` |  
| Установка браузеров | Установите `@playwright/browser-chromium`, `@playwright/browser-firefox` и/или `@playwright/browser-webkit` | `npx playwright install` или `npx playwright install chromium` для одного браузера |  
| `import` из | `playwright` | `@playwright/test` |  
| Инициализация | Необходимо явно: <ol><li>Выбрать браузер, например `chromium`</li><li>Запустить его с помощью [browserType.launch()](/api/class-browsertype.mdx#browser-type-launch)</li><li>Создать контекст с [browser.newContext()](/api/class-browser.mdx#browser-new-context) и передать параметры, например `devices['iPhone 11']`</li><li>Создать страницу с [browserContext.newPage()](/api/class-browsercontext.mdx#browser-context-new-page)</li></ol> | `page` и `context` автоматически создаются для каждого теста, а также доступны [встроенные фикстуры](./test-fixtures.mdx#built-in-fixtures) |  
| Утверждения | Нет встроенных Web-First утверждений | [Web-First утверждения](./test-assertions.mdx), которые автоматически ожидают выполнения условия |  
| Тайм-ауты | По умолчанию 30 секунд для большинства операций | Большинство операций не имеют тайм-аута, но сам тест завершится с ошибкой, если выполнится дольше 30 секунд |  
| Очистка | Необходимо явно закрывать: <ol><li>Контекст с [browserContext.close()](/api/class-browsercontext.mdx#browser-context-close)</li><li>Браузер с [browser.close()](/api/class-browser.mdx#browser-close)</li></ol> | Встроенные фикстуры автоматически закрываются тестовым раннером |  
| Запуск | Код запускается как Node.js-скрипт | Используется `npx playwright test`, который автоматически обрабатывает компиляцию и выбор тестов |  

Кроме того, Playwright Test включает:
- [Конфигурацию проектов](./test-configuration.mdx)
- [Параллельный запуск тестов](./test-parallel.mdx)
- [Web-First утверждения](./test-assertions.mdx)
- [Генерацию отчетов](./test-reporters.mdx)
- [Автоматический повтор тестов](./test-retries.mdx)
- [Поддержку трассировки](./trace-viewer-intro.mdx)

## Использование

Установите Playwright с помощью npm или Yarn. См. [системные требования](./intro.mdx#system-requirements).

```bash
npm i -D playwright
```  

Также потребуется установка браузеров:

```bash
# Установка Chromium, Firefox и WebKit
npx playwright install chromium firefox webkit

# Либо установка пакетов, автоматически загружающих браузеры
npm i -D @playwright/browser-chromium @playwright/browser-firefox @playwright/browser-webkit
```  

См. [управление браузерами](./browsers.mdx#managing-browser-binaries) для дополнительных настроек.

После установки можно импортировать Playwright в скрипт и запустить браузер:

```js
const { chromium } = require('playwright');

(async () => {
  const browser = await chromium.launch();
  // Работа с браузером
  await browser.close();
})();
```  

## Первый скрипт

В этом примере мы откроем `https://playwright.dev/` и сделаем скриншот в WebKit.

```js
const { webkit } = require('playwright');

(async () => {
  const browser = await webkit.launch();
  const page = await browser.newPage();
  await page.goto('https://playwright.dev/');
  await page.screenshot({ path: `example.png` });
  await browser.close();
})();
```

По умолчанию Playwright запускает браузеры в headless-режиме. Чтобы увидеть интерфейс браузера, передайте флаг `headless: false` при запуске. Можно также использовать `slowMo`, чтобы замедлить выполнение команд. Подробнее см. в разделе [инструменты отладки](./debug.mdx).

```js
firefox.launch({ headless: false, slowMo: 50 });
```

## Запись скриптов

[Инструменты командной строки](./test-cli.mdx) позволяют записывать пользовательские действия и генерировать JavaScript-код.

```bash
npx playwright codegen wikipedia.org
```

## Загрузка браузеров

Чтобы загрузить браузеры Playwright, выполните:

```bash
# Явная загрузка браузеров
npx playwright install
```

Также можно установить пакеты `@playwright/browser-chromium`, `@playwright/browser-firefox` и `@playwright/browser-webkit`, которые автоматически загружают соответствующие браузеры во время установки.

```bash
# Использование вспомогательного пакета для загрузки браузеров при установке npm
npm install @playwright/browser-chromium
```

**Загрузка через прокси или брандмауэр**

Чтобы загрузить браузеры через прокси, укажите переменную окружения `HTTPS_PROXY`.

<Tabs groupId="bash-flavor" defaultValue="bash" values={[ {label: 'Bash', value: 'bash'}, {label: 'PowerShell', value: 'powershell'}, {label: 'Batch', value: 'batch'} ]}>
    <TabItem value="bash">

        ```bash
        # Явная загрузка
        HTTPS_PROXY=https://192.0.2.1 npx playwright install

        # Через вспомогательные пакеты
        HTTPS_PROXY=https://192.0.2.1 npm install
        ```

    </TabItem>
    <TabItem value="powershell">

        ```powershell
        # Явная загрузка
        $Env:HTTPS_PROXY=https://192.0.2.1
        npx playwright install

        # Через вспомогательные пакеты
        $Env:HTTPS_PROXY=https://192.0.2.1
        npm install
        ```

    </TabItem>
    <TabItem value="batch">

        ```batch
        # Явная загрузка
        set HTTPS_PROXY=https://192.0.2.1
        npx playwright install

        # Через вспомогательные пакеты
        set HTTPS_PROXY=https://192.0.2.1
        npm install
        ```

    </TabItem>
</Tabs>

**Загрузка из репозитория артефактов**

По умолчанию Playwright загружает браузеры из CDN Microsoft. Чтобы загрузить их из внутреннего репозитория, используйте переменную `PLAYWRIGHT_DOWNLOAD_HOST`.

<Tabs groupId="bash-flavor" defaultValue="bash" values={[ {label: 'Bash', value: 'bash'}, {label: 'PowerShell', value: 'powershell'}, {label: 'Batch', value: 'batch'} ]}>
    <TabItem value="bash">

        ```bash
        # Явная загрузка
        PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1 npx playwright install

        # Через вспомогательные пакеты
        PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1 npm install
        ```

    </TabItem>
    <TabItem value="powershell">

        ```powershell
        # Явная загрузка
        $Env:PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1
        npx playwright install

        # Через вспомогательные пакеты
        $Env:PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1
        npm install
        ```

    </TabItem>
    <TabItem value="batch">

        ```batch
        # Явная загрузка
        set PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1
        npx playwright install

        # Через вспомогательные пакеты
        set PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1
        npm install
        ```

    </TabItem>
</Tabs>

**Пропуск загрузки браузеров**

В некоторых случаях браузеры загружаются и управляются отдельно, поэтому их загрузку можно отключить, установив переменную `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD` перед установкой пакетов.

<Tabs groupId="bash-flavor" defaultValue="bash" values={[ {label: 'Bash', value: 'bash'}, {label: 'PowerShell', value: 'powershell'}, {label: 'Batch', value: 'batch'} ]}>
    <TabItem value="bash">

        ```bash
        # Отключение загрузки браузеров при установке вспомогательных пакетов
        PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install
        ```

    </TabItem>
    <TabItem value="powershell">

        ```powershell
        # Отключение загрузки браузеров при установке вспомогательных пакетов
        $Env:PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
        npm install
        ```

    </TabItem>
    <TabItem value="batch">

        ```batch
        # Отключение загрузки браузеров при установке вспомогательных пакетов
        set PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
        npm install
        ```

    </TabItem>
</Tabs>

## Поддержка TypeScript

Playwright включает встроенную поддержку TypeScript. Определения типов импортируются автоматически. Рекомендуется использовать проверку типов для улучшения работы с редактором кода.

### В JavaScript

Добавьте следующую строку в начало JavaScript-файла, чтобы включить проверку типов в VS Code или WebStorm:

```js
// @ts-check
// ...
```

Также можно использовать JSDoc для явного указания типов переменных:

```js
/** @type {import('playwright').Page} */
let page;
```

### В TypeScript

Поддержка TypeScript работает из коробки. Можно также импортировать типы вручную:

```js
let page: import('playwright').Page;
```
