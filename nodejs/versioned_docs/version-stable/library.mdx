---
id: library
title: "Библиотека"
---
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import HTMLCard from '@site/src/components/HTMLCard';

## Введение

Библиотека Playwright предоставляет унифицированные API для запуска и взаимодействия с браузерами, в то время как Playwright Test предлагает все это, а также полностью управляемый тестовый раннер и опыт работы с ним.

В большинстве случаев для сквозного тестирования вам следует использовать `@playwright/test` (Playwright Test), а не `playwright` (Библиотека Playwright) напрямую. Чтобы начать работу с Playwright Test, следуйте [Руководству по началу работы](./intro.mdx).

## Различия при использовании библиотеки

### Пример библиотеки

Следующий пример демонстрирует использование Библиотеки Playwright напрямую для запуска Chromium, перехода на страницу и проверки ее заголовка:

<Tabs
  groupId="js-flavor"
  defaultValue="ts"
  values={[
    {label: 'TypeScript', value: 'ts'},
    {label: 'JavaScript', value: 'js'}
  ]
}>
<TabItem value="ts">

```js
import { chromium, devices } from 'playwright';
import assert from 'node:assert';

(async () => {
  // Настройка
  const browser = await chromium.launch();
  const context = await browser.newContext(devices['iPhone 11']);
  const page = await context.newPage();

  // Действительная интересная часть
  await context.route('**.jpg', route => route.abort());
  await page.goto('https://example.com/');

  assert(await page.title() === 'Example Domain'); // 👎 не веб-ориентированное утверждение

  // Завершение работы
  await context.close();
  await browser.close();
})();
```

</TabItem>
<TabItem value="js">

```js
const assert = require('node:assert');
const { chromium, devices } = require('playwright');

(async () => {
  // Настройка
  const browser = await chromium.launch();
  const context = await browser.newContext(devices['iPhone 11']);
  const page = await context.newPage();

  // Действительная интересная часть
  await context.route('**.jpg', route => route.abort());
  await page.goto('https://example.com/');

  assert(await page.title() === 'Example Domain'); // 👎 не веб-ориентированное утверждение

  // Завершение работы
  await context.close();
  await browser.close();
})();
```

</TabItem>
</Tabs>

Запустите его с помощью `node my-script.js`.

### Пример теста

Тест для достижения аналогичного поведения будет выглядеть следующим образом:

<Tabs
  groupId="js-flavor"
  defaultValue="ts"
  values={[
    {label: 'TypeScript', value: 'ts'},
    {label: 'JavaScript', value: 'js'}
  ]
}>
<TabItem value="ts">

```js
import { expect, test, devices } from '@playwright/test';

test.use(devices['iPhone 11']);

test('должен иметь заголовок', async ({ page, context }) => {
  await context.route('**.jpg', route => route.abort());
  await page.goto('https://example.com/');

  await expect(page).toHaveTitle('Example');
});
```

</TabItem>
<TabItem value="js">

```js
const { expect, test, devices } = require('@playwright/test');

test.use(devices['iPhone 11']);

test('должен иметь заголовок', async ({ page, context }) => {
  await context.route('**.jpg', route => route.abort());
  await page.goto('https://example.com/');

  await expect(page).toHaveTitle('Example');
});
```

</TabItem>
</Tabs>

Запустите его с помощью `npx playwright test`.

### Ключевые различия

Ключевые различия, которые следует отметить, следующие:

| | Библиотека | Тест |
| - | - | - |
| Установка | `npm install playwright` | `npm init playwright@latest` - обратите внимание на `install` и `init` |
| Установка браузеров | Установите `@playwright/browser-chromium`, `@playwright/browser-firefox` и/или `@playwright/browser-webkit` | `npx playwright install` или `npx playwright install chromium` для одного браузера |
| `import` из | `playwright` | `@playwright/test` |
| Инициализация | Явно необходимо: <ol><li>Выбрать браузер для использования, например, `chromium`</li><li>Запустить браузер с помощью [browserType.launch()](/api/class-browsertype.mdx#browser-type-launch)</li><li>Создать контекст с помощью [browser.newContext()](/api/class-browser.mdx#browser-new-context), <em>и</em> передать любые параметры контекста явно, например, `devices['iPhone 11']`</li><li>Создать страницу с помощью [browserContext.newPage()](/api/class-browsercontext.mdx#browser-context-new-page)</li></ol> | Изолированные `page` и `context` предоставляются каждому тесту из коробки, вместе с другими [встроенными фикстурами](./test-fixtures.mdx#built-in-fixtures). Нет явного создания. Если они упоминаются в аргументах теста, тестовый раннер создаст их для теста. (т.е. ленивое инициализация) |
| Утверждения | Нет встроенных веб-ориентированных утверждений | [Веб-ориентированные утверждения](./test-assertions.mdx) такие как: <ul><li>[expect(page).toHaveTitle()](/api/class-pageassertions.mdx#page-assertions-to-have-title)</li><li>[expect(page).toHaveScreenshot()](/api/class-pageassertions.mdx#page-assertions-to-have-screenshot-1)</li></ul> которые автоматически ждут и повторяют попытки, пока условие не будет выполнено.|
| Таймауты | По умолчанию 30 секунд для большинства операций. | Большинство операций не имеют таймаута, но каждый тест имеет таймаут, который делает его неудачным (по умолчанию 30 секунд). |
| Очистка | Явно необходимо: <ol><li>Закрыть контекст с помощью [browserContext.close()](/api/class-browsercontext.mdx#browser-context-close)</li><li>Закрыть браузер с помощью [browser.close()](/api/class-browser.mdx#browser-close)</li></ol> | Нет явного закрытия [встроенных фикстур](./test-fixtures.mdx#built-in-fixtures); тестовый раннер позаботится об этом.
| Запуск | При использовании библиотеки вы запускаете код как скрипт node, возможно, с предварительной компиляцией. | При использовании тестового раннера вы используете команду `npx playwright test`. Вместе с вашей [конфигурацией](./test-configuration.mdx) тестовый раннер обрабатывает любую компиляцию и выбирает, что запускать и как это делать. |

В дополнение к вышеизложенному, Playwright Test, как полнофункциональный тестовый раннер, включает:
- [Матрица конфигурации и проекты](./test-configuration.mdx): В приведенном выше примере, в версии Библиотеки Playwright, если бы мы хотели запустить с другим устройством или браузером, нам пришлось бы изменить скрипт и передать информацию. С Playwright Test мы можем просто указать [матрицу конфигураций](./test-configuration.mdx) в одном месте, и он запустит один тест под каждой из этих конфигураций.
- [Параллелизация](./test-parallel.mdx)
- [Веб-ориентированные утверждения](./test-assertions.mdx)
- [Отчетность](./test-reporters.mdx)
- [Повторы](./test-retries.mdx)
- [Легко включаемое трассирование](./trace-viewer-intro.mdx)
- и многое другое…

## Использование

Используйте npm или Yarn для установки библиотеки Playwright в вашем проекте Node.js. См. [системные требования](./intro.mdx#system-requirements).

```bash
npm i -D playwright
```

Вам также нужно будет установить браузеры - либо вручную, либо добавив пакет, который сделает это за вас автоматически.

```bash
# Загрузить браузеры Chromium, Firefox и WebKit
npx playwright install chromium firefox webkit

# Либо добавьте пакеты, которые загрузят браузер при установке npm
npm i -D @playwright/browser-chromium @playwright/browser-firefox @playwright/browser-webkit
```

См. [управление браузерами](./browsers.mdx#managing-browser-binaries) для получения дополнительных вариантов.

После установки вы можете импортировать Playwright в скрипт Node.js и запустить любой из 3 браузеров (`chromium`, `firefox` и `webkit`).

```js
const { chromium } = require('playwright');

(async () => {
  const browser = await chromium.launch();
  // Создайте страницы, взаимодействуйте с элементами UI, утверждайте значения
  await browser.close();
})();
```

API Playwright являются асинхронными и возвращают объекты Promise. Наши примеры кода используют [парадигму async/await](https://developer.mozilla.org/en-US/docs/Learn/JavaScript/Asynchronous/Async_await) для облегчения чтения. Код обернут в анонимную асинхронную стрелочную функцию, которая вызывает саму себя.

```js
(async () => { // Начало асинхронной стрелочной функции
  // Код функции
  // ...
})(); // Конец функции и () для вызова самой себя
```

## Первый скрипт

В нашем первом скрипте мы перейдем на `https://playwright.dev/` и сделаем скриншот в WebKit.

```js
const { webkit } = require('playwright');

(async () => {
  const browser = await webkit.launch();
  const page = await browser.newPage();
  await page.goto('https://playwright.dev/');
  await page.screenshot({ path: `example.png` });
  await browser.close();
})();
```

По умолчанию Playwright запускает браузеры в безголовом режиме. Чтобы увидеть интерфейс браузера, передайте флаг `headless: false` при запуске браузера. Вы также можете использовать `slowMo`, чтобы замедлить выполнение. Узнайте больше в разделе инструментов отладки [раздел](./debug.mdx).

```js
firefox.launch({ headless: false, slowMo: 50 });
```

## Запись скриптов

[Инструменты командной строки](./test-cli.mdx) могут использоваться для записи взаимодействий пользователя и генерации кода JavaScript.

```bash
npx playwright codegen wikipedia.org
```

## Загрузки браузеров

Чтобы загрузить браузеры Playwright, выполните:

```bash
# Явно загрузить браузеры
npx playwright install
```

В качестве альтернативы вы можете добавить пакеты `@playwright/browser-chromium`, `@playwright/browser-firefox` и `@playwright/browser-webkit`, чтобы автоматически загружать соответствующий браузер во время установки пакета.

```bash
# Используйте вспомогательный пакет, который загружает браузер при установке npm
npm install @playwright/browser-chromium
```

**Загрузка за файрволом или прокси**

Передайте переменную окружения `HTTPS_PROXY`, чтобы загрузить через прокси.

<Tabs
  groupId="bash-flavor"
  defaultValue="bash"
  values={[
    {label: 'Bash', value: 'bash'},
    {label: 'PowerShell', value: 'powershell'},
    {label: 'Batch', value: 'batch'}
  ]
}>
<TabItem value="bash">

```bash
# Вручную
HTTPS_PROXY=https://192.0.2.1 npx playwright install

# Через пакеты @playwright/browser-chromium, @playwright/browser-firefox
# и @playwright/browser-webkit
HTTPS_PROXY=https://192.0.2.1 npm install
```

</TabItem>
<TabItem value="powershell">

```powershell
# Вручную
$Env:HTTPS_PROXY=https://192.0.2.1
npx playwright install

# Через пакеты @playwright/browser-chromium, @playwright/browser-firefox
# и @playwright/browser-webkit
$Env:HTTPS_PROXY=https://192.0.2.1
npm install
```

</TabItem>
<TabItem value="batch">

```batch
# Вручную
set HTTPS_PROXY=https://192.0.2.1
npx playwright install

# Через пакеты @playwright/browser-chromium, @playwright/browser-firefox
# и @playwright/browser-webkit
set HTTPS_PROXY=https://192.0.2.1
npm install
```

</TabItem>
</Tabs>

**Загрузка из репозитория артефактов**

По умолчанию Playwright загружает браузеры из CDN Microsoft. Передайте переменную окружения `PLAYWRIGHT_DOWNLOAD_HOST`, чтобы загрузить из внутреннего репозитория артефактов.

<Tabs
  groupId="bash-flavor"
  defaultValue="bash"
  values={[
    {label: 'Bash', value: 'bash'},
    {label: 'PowerShell', value: 'powershell'},
    {label: 'Batch', value: 'batch'}
  ]
}>
<TabItem value="bash">

```bash
# Вручную
PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1 npx playwright install

# Через пакеты @playwright/browser-chromium, @playwright/browser-firefox
# и @playwright/browser-webkit
PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1 npm install
```

</TabItem>
<TabItem value="powershell">

```powershell
# Вручную
$Env:PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1
npx playwright install

# Через пакеты @playwright/browser-chromium, @playwright/browser-firefox
# и @playwright/browser-webkit
$Env:PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1
npm install
```

</TabItem>
<TabItem value="batch">

```batch
# Вручную
set PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1
npx playwright install

# Через пакеты @playwright/browser-chromium, @playwright/browser-firefox
# и @playwright/browser-webkit
set PLAYWRIGHT_DOWNLOAD_HOST=192.0.2.1
npm install
```

</TabItem>
</Tabs>

**Пропустить загрузку браузера**

В некоторых случаях может потребоваться полностью избежать загрузки браузеров, поскольку бинарные файлы браузеров управляются отдельно. Это можно сделать, установив переменную `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD` перед установкой пакетов.

<Tabs
  groupId="bash-flavor"
  defaultValue="bash"
  values={[
    {label: 'Bash', value: 'bash'},
    {label: 'PowerShell', value: 'powershell'},
    {label: 'Batch', value: 'batch'}
  ]
}>
<TabItem value="bash">

```bash
# При использовании пакетов @playwright/browser-chromium, @playwright/browser-firefox
# и @playwright/browser-webkit
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm install
```

</TabItem>
<TabItem value="powershell">

```powershell
# При использовании пакетов @playwright/browser-chromium, @playwright/browser-firefox
# и @playwright/browser-webkit
$Env:PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
npm install
```

</TabItem>
<TabItem value="batch">

```batch
# При использовании пакетов @playwright/browser-chromium, @playwright/browser-firefox
# и @playwright/browser-webkit
set PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
npm install
```

</TabItem>
</Tabs>

## Поддержка TypeScript

Playwright включает встроенную поддержку TypeScript. Типы будут импортированы автоматически. Рекомендуется использовать проверку типов для улучшения работы с IDE.

### В JavaScript

Добавьте следующее в начало вашего JavaScript файла, чтобы получить проверку типов в VS Code или WebStorm.

```js
// @ts-check
// ...
```

В качестве альтернативы вы можете использовать JSDoc для установки типов для переменных.

```js
/** @type {import('playwright').Page} */
let page;
```

### В TypeScript

Поддержка TypeScript будет работать из коробки. Типы также могут быть импортированы явно.

```js
let page: import('playwright').Page;
```